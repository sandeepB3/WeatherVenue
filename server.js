// const { port, REDIS_PORT, OPENWEATHERMAP_API_KEY, GOOGLE_MAPS_API_KEY, HONEYPOT_KEY } = require('./variables.env')
// console.log( `Your port is ${port} Your OWMkey is ${OPENWEATHERMAP_API_KEY})

const { port } = require('./variables.env')
console.log(`Your port is ${port}`)
